<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Settings.ExcludeProcessesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LenovoLegionToolkit.WPF.Controls"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{x:Static resources:Resource.ExcludeProcessesWindow_Title}"
    Width="460"
    Height="580"
    MinWidth="460"
    MinHeight="580"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="CanMinimize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="false"
            ShowMaximize="false"
            ShowMinimize="false"
            UseSnapLayout="false" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="12"
                IsHitTestVisible="False"
                Text="{x:Static resources:Resource.ExcludeProcessesWindow_Title}" />

        </Grid>

        <controls:LoadableControl
            x:Name="_loader"
            Grid.Row="1"
            Margin="12,0,12,12"
            IndicatorVerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <custom:HintText
                    Grid.Row="0"
                    Margin="0,0,0,8"
                    Text="{x:Static resources:Resource.ExcludeProcessesWindow_Description}" />

                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <wpfui:TextBox
                        x:Name="_searchBox"
                        Grid.Column="0"
                        Icon="Search24"
                        PlaceholderText="{x:Static resources:Resource.Filter}"
                        TextChanged="SearchBox_TextChanged" />

                    <wpfui:Button
                        x:Name="_refreshButton"
                        Grid.Column="1"
                        Width="{Binding ActualHeight, ElementName=_searchBox}"
                        Height="{Binding ActualHeight, ElementName=_searchBox}"
                        Margin="4,0,0,0"
                        Padding="0"
                        Appearance="Secondary"
                        Click="RefreshButton_Click"
                        Cursor="Hand"
                        Icon="ArrowClockwise24"
                        ToolTip="{x:Static resources:Resource.Refresh}" />
                </Grid>

                <wpfui:DynamicScrollViewer Grid.Row="2">
                    <StackPanel Margin="0,0,16,0">
                        <Expander
                            x:Name="_includedExpander"
                            Margin="0,0,0,12"
                            Header="{x:Static resources:Resource.ExcludeProcessesWindow_Title}"
                            Style="{StaticResource SectionExpanderStyle}"
                            IsExpanded="True">
                            <Border CornerRadius="8">
                                <Border.Background>
                                    <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" />
                                </Border.Background>
                                <Grid>
                                    <TextBlock
                                        x:Name="_noIncludedProcessesText"
                                        Margin="12"
                                        FontStyle="Italic"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Text="{x:Static resources:Resource.ExcludeProcessesWindow_NoIncludedProcesses}"
                                        Visibility="Collapsed" />
                                    <ItemsControl
                                        x:Name="_includedList"
                                        Margin="8,8,8,0"
                                        HorizontalContentAlignment="Stretch"
                                        ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid Grid.Column="0" Width="24" Height="24" Margin="0,0,12,0">
                                                        <wpfui:SymbolIcon Symbol="AppGeneric24" FontSize="18" Foreground="{DynamicResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        <Image Source="{Binding Icon}" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality" />
                                                    </Grid>
                                                    <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Medium" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                                    <wpfui:Button Grid.Column="2" Appearance="Transparent" Icon="Dismiss24" CommandParameter="{Binding}" Click="RemoveIncludedProcess_Click" ToolTip="{x:Static resources:Resource.Remove}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </Expander>

                        <Expander
                            x:Name="_excludedExpander"
                            Margin="0,0,0,12"
                            Header="{x:Static resources:Resource.ExcludeProcessesWindow_Title}"
                            Style="{StaticResource SectionExpanderStyle}"
                            IsExpanded="True">
                            <Border CornerRadius="8">
                                <Border.Background>
                                    <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" />
                                </Border.Background>
                                <Grid>
                                    <TextBlock
                                        x:Name="_noExcludedProcessesText"
                                        Margin="12"
                                        FontStyle="Italic"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Text="{x:Static resources:Resource.ExcludeProcessesWindow_NoExcludedProcesses}"
                                        Visibility="Collapsed" />
                                    <ItemsControl
                                        x:Name="_excludedList"
                                        Margin="8,8,8,0"
                                        HorizontalContentAlignment="Stretch"
                                        ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid Grid.Column="0" Width="24" Height="24" Margin="0,0,12,0">
                                                        <wpfui:SymbolIcon Symbol="AppGeneric24" FontSize="18" Foreground="{DynamicResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        <Image Source="{Binding Icon}" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality" />
                                                    </Grid>
                                                    <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Medium" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                                    <wpfui:Button Grid.Column="2" Appearance="Transparent" Icon="Dismiss24" CommandParameter="{Binding}" Click="RemoveExcludedProcess_Click" ToolTip="{x:Static resources:Resource.Remove}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </Expander>

                        <Expander
                            x:Name="_runningExpander"
                            Margin="0,0,0,8"
                            Header="{x:Static resources:Resource.ExcludeProcessesWindow_Title}"
                            Style="{StaticResource SectionExpanderStyle}"
                            IsExpanded="True">
                            <Border CornerRadius="8">
                                <Border.Background>
                                    <SolidColorBrush Color="{DynamicResource ControlFillColorDefault}" />
                                </Border.Background>
                                <ItemsControl
                                    x:Name="_runningList"
                                    Margin="8,8,8,0"
                                    HorizontalContentAlignment="Stretch"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Grid Grid.Column="0" Width="24" Height="24" Margin="0,0,12,0">
                                                    <wpfui:SymbolIcon Symbol="AppGeneric24" FontSize="18" Foreground="{DynamicResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    <Image Source="{Binding Icon}" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality" />
                                                </Grid>
                                                <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Medium" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" />
                                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                    <wpfui:Button Margin="0,0,4,0" Appearance="Transparent" Icon="Add24" CommandParameter="{Binding}" Click="IncludeProcess_Click" ToolTip="{x:Static resources:Resource.ExcludeProcessesWindow_IncludeTooltip}" />
                                                    <wpfui:Button Appearance="Transparent" Icon="Subtract24" CommandParameter="{Binding}" Click="ExcludeProcess_Click" ToolTip="{x:Static resources:Resource.ExcludeProcessesWindow_ExcludeTooltip}" />
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Border>
                        </Expander>
                    </StackPanel>
                </wpfui:DynamicScrollViewer>

                <Grid Grid.Row="3" Margin="0,8,16,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <wpfui:Button
                        Grid.Column="1"
                        MinWidth="100"
                        Appearance="Primary"
                        Click="SaveButton_Click"
                        Content="{x:Static resources:Resource.Save}" />

                    <wpfui:Button
                        Grid.Column="2"
                        MinWidth="100"
                        Margin="8,0,0,0"
                        Click="CancelButton_Click"
                        Content="{x:Static resources:Resource.Cancel}" />

                </Grid>

            </Grid>
        </controls:LoadableControl>

    </Grid>

</local:BaseWindow>
