<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Utils.DeviceInformationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:LenovoLegionToolkit.WPF.Controls"
    xmlns:custom="clr-namespace:LenovoLegionToolkit.WPF.Controls.Custom"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{x:Static resources:Resource.DeviceInformationWindow_Title}"
    Width="950"
    Height="570"
    ExtendsContentIntoTitleBar="True"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    Loaded="DeviceInformationWindow_Loaded"
    ResizeMode="CanMinimize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="false"
            ShowMaximize="false"
            ShowMinimize="false"
            UseSnapLayout="false" />

        <Grid
            Grid.Row="0"
            Height="24"
            Margin="12,8,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <wpfui:SymbolIcon Symbol="Laptop24" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="16" Margin="0,0,8,0" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    IsHitTestVisible="False"
                    Text="{x:Static resources:Resource.DeviceInformationWindow_Title}" />
            </StackPanel>
        </Grid>

        <wpfui:DynamicScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="16,0,16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left Column -->
                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <!-- Device Details -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="Laptop24" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.Device}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            
                            <TextBlock x:Name="_modelLargeLabel" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,12" />

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="Window24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock x:Name="_osVersionLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="Key24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                        <Run Text="{x:Static resources:Resource.SerialNumber}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_serialNumberLabel" Margin="0,0,8,0" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" VerticalAlignment="Center" />
                                </StackPanel>
                                <wpfui:Button x:Name="_toggleSerialNumberButton" Grid.Column="2" Padding="4" Appearance="Transparent" Cursor="Hand" Click="ToggleSerialNumberButton_OnClick" Icon="Eye24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,4,0" />
                                <wpfui:Button x:Name="_copySerialNumberButton" Grid.Column="3" Padding="4" Appearance="Transparent" Cursor="Hand" Click="CopyButton_OnClick" Icon="Copy24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Tag="{Binding ElementName=_serialNumberLabel, Path=Text}" />
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="Tag24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                        <Run Text="{x:Static resources:Resource.Model}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_modelLabel" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" />
                                </StackPanel>
                                <wpfui:Button x:Name="_copyModelButton" Grid.Column="2" Padding="4" Appearance="Transparent" Cursor="Hand" Click="CopyButton_OnClick" Icon="Copy24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Tag="{Binding ElementName=_modelLabel, Path=Text}" />
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="NumberSymbol24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                        <Run Text="{x:Static resources:Resource.MachineType}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_mtmLabel" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" />
                                </StackPanel>
                                <wpfui:Button x:Name="_copyMtmButton" Grid.Column="2" Padding="4" Appearance="Transparent" Cursor="Hand" Click="CopyButton_OnClick" Icon="Copy24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Tag="{Binding ElementName=_mtmLabel, Path=Text}" />
                            </Grid>

                            <Grid x:Name="_biosCard" Background="Transparent" MouseLeftButtonUp="BiosCard_OnMouseLeftButtonUp">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="Code24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                        <Run Text="{x:Static resources:Resource.BiosVersion}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_biosLabel" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" />
                                </StackPanel>
                                <wpfui:Button x:Name="_copyBiosButton" Grid.Column="2" Padding="4" Appearance="Transparent" Cursor="Hand" Click="CopyButton_OnClick" Icon="Copy24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Tag="{Binding ElementName=_biosLabel, Path=Text}" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Warranty -->
                    <Border x:Name="_warrantyInfo" Visibility="Collapsed" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" Grid.Column="0">
                                    <wpfui:SymbolIcon x:Name="_warrantyStatusIcon" Symbol="CheckmarkCircle24" Foreground="{DynamicResource PaletteGreenBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock Text="{x:Static resources:Resource.Warranty}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                                </StackPanel>
                                <wpfui:Button x:Name="_refreshWarrantyButton" Grid.Column="1" Padding="4" Appearance="Transparent" Cursor="Hand" Click="RefreshWarrantyButton_OnClick" Icon="ArrowClockwise24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" ToolTip="{x:Static resources:Resource.Refresh}" />
                            </Grid>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock x:Name="_warrantyStatusLabel" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PaletteGreenBrush}" />
                            </StackPanel>

                            <Border CornerRadius="3" ClipToBounds="True" Height="6" Background="{DynamicResource ControlStrokeColorDefaultBrush}" Margin="0,8,0,12">
                                <ProgressBar x:Name="_warrantyProgressBar" Value="100" BorderThickness="0" Background="Transparent">
                                    <ProgressBar.Foreground>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="{StaticResource PaletteGreenColor}" Offset="0" />
                                            <GradientStop Color="{StaticResource PaletteTealColor}" Offset="1" />
                                        </LinearGradientBrush>
                                    </ProgressBar.Foreground>
                                </ProgressBar>
                            </Border>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.StartDate}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_warrantyStartLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>
                            
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.EndDate}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_warrantyEndLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>
                            
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.DaysRemaining}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_warrantyDaysRemainingLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource PaletteGreenBrush}" FontWeight="Medium" />
                            </Grid>
                            
                            <wpfui:Button x:Name="_warrantyLinkCardAction" Appearance="Transparent" Cursor="Hand" BorderThickness="0" HorizontalAlignment="Left" Padding="0" Margin="0,10,0,0" Click="WarrantyLinkCardAction_OnClick" Content="{x:Static resources:Resource.DeviceInformationWindow_LenovoSupport}" Icon="Open24" IsEnabled="False" Foreground="{DynamicResource PaletteLightBlueBrush}" />
                        </StackPanel>
                    </Border>

                    <!-- Platform & Capabilities -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="Info24" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.PlatformAndCapabilities}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>

                            <Grid Margin="0,0,0,10" x:Name="_seriesRow">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="Layer24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                        <Run Text="{x:Static resources:Resource.Series}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_seriesLabel" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" />
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,0,0,10" x:Name="_generationRow">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="History24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                        <Run Text="{x:Static resources:Resource.Generation}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_generationLabel" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" />
                                </StackPanel>
                            </Grid>



                            <Grid Margin="0,0,0,10" x:Name="_modesRow">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="Gauge24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Top" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4">
                                        <Run Text="{x:Static resources:Resource.PowerModes}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_powerModesLabel" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>

                            <Grid x:Name="_capabilitiesRow">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <wpfui:SymbolIcon Grid.Column="0" Symbol="DeveloperBoard24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Top" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Top">
                                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4">
                                        <Run Text="{x:Static resources:Resource.HardwareFeatures}" /><Run Text=":" />
                                    </TextBlock>
                                    <TextBlock x:Name="_capabilitiesLabel" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontWeight="Medium" Focusable="True" TextWrapping="Wrap" MaxHeight="35" TextTrimming="CharacterEllipsis" />
                                    <ToggleButton x:Name="_expandCapabilitiesButton" Content="{x:Static resources:Resource.ShowMore}" Background="Transparent" Cursor="Hand" BorderThickness="0" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="12" Padding="0,4" HorizontalAlignment="Left" Click="ExpandCapabilities_OnClick" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                </StackPanel>

                <!-- Middle Column -->
                <StackPanel Grid.Column="1" Margin="5,0,5,0">

                    <!-- CPU -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon x:Name="_cpuIcon" Symbol="DeveloperBoard24" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.CPU}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock x:Name="_cpuNameLabel" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Focusable="True" TextWrapping="Wrap" />
                            <TextBlock x:Name="_cpuDetailsLabel" FontSize="12" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Focusable="True" />
                        </StackPanel>
                    </Border>

                    <!-- GPU -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon x:Name="_gpuIcon" Symbol="Games24" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.GPU}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <ItemsControl x:Name="_gpuList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,8">
                                            <TextBlock Text="{Binding Name, TargetNullValue={x:Static resources:Resource.Unknown}}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" />
                                            <TextBlock FontSize="12" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                                <Run Text="{Binding Vram}" />
                                                <Run Text=" " />
                                                <Run Text="{x:Static resources:Resource.GB}" />
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- RAM -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="Database24" Foreground="{DynamicResource PalettePurpleBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.RAM}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <ItemsControl x:Name="_ramList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                                                    <Run Text="{Binding CapacityGb, StringFormat=\{0:F0\}}" />
                                                    <Run Text=" " />
                                                    <Run Text="{x:Static resources:Resource.GB}" />
                                                    <Run Text=" " />
                                                    <Run Text="{Binding Generation, TargetNullValue={x:Static resources:Resource.Unknown}}" />
                                                </TextBlock>
                                                <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12">
                                                    <Run Text="{x:Static resources:Resource.Slot}" />
                                                    <Run Text=" " />
                                                    <Run Text="{Binding Slot}" />
                                                    <Run Text=" • " />
                                                    <Run Text="{Binding Manufacturer, TargetNullValue={x:Static resources:Resource.Unknown}}" />
                                                </TextBlock>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" FontSize="13" FontWeight="Medium" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center">
                                                <Run Text="{Binding SpeedMhz, TargetNullValue={x:Static resources:Resource.Unknown}}" />
                                                <Run Text=" " />
                                                <Run Text="{x:Static resources:Resource.MHz}" />
                                            </TextBlock>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Motherboard -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="DeveloperBoard24" Foreground="{DynamicResource PaletteGreenBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.Motherboard}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock x:Name="_motherboardNameLabel" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Focusable="True" />
                                    <TextBlock x:Name="_motherboardDetailsLabel" FontSize="12" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Focusable="True" />
                                </StackPanel>
                                <wpfui:Button x:Name="_copyMotherboardButton" Grid.Column="1" Padding="4" Appearance="Transparent" Cursor="Hand" Click="CopyButton_OnClick" Icon="Copy24" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Tag="{Binding ElementName=_motherboardNameLabel, Path=Text}" />
                            </Grid>
                        </StackPanel>
                    </Border>


                </StackPanel>

                <!-- Right Column -->
                <StackPanel Grid.Column="2" Margin="5,0,0,0">
                    <!-- Storage -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="HardDrive20" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.Storage}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <ItemsControl x:Name="_diskList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Name, TargetNullValue={x:Static resources:Resource.Unknown}}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" />
                                                    <TextBlock Text="{Binding Label}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center">
                                                    <Run Text="{Binding UsedGb, StringFormat=\{0:F1\}, Mode=OneWay}" />
                                                    <Run Text=" " />
                                                    <Run Text="{x:Static resources:Resource.GB}" />
                                                    <Run Text=" " />
                                                    <Run Text="{x:Static resources:Resource.Used}" />
                                                    <Run Text=" / " />
                                                    <Run Text="{Binding SizeGb, StringFormat=\{0:F1\}, Mode=OneWay}" />
                                                    <Run Text=" " />
                                                    <Run Text="{x:Static resources:Resource.GB}" />
                                                </TextBlock>
                                            </Grid>
                                            <Border CornerRadius="3" ClipToBounds="True" Height="6" Background="{DynamicResource ControlStrokeColorDefaultBrush}" Margin="0,8,0,0">
                                                <ProgressBar Value="{Binding UsedGb, Mode=OneWay}" Maximum="{Binding SizeGb, Mode=OneWay}" BorderThickness="0" Background="Transparent" x:Name="StorageProgressBar">
                                                    <ProgressBar.Foreground>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="{StaticResource PaletteLightBlueColor}" Offset="0" />
                                                            <GradientStop Color="{StaticResource PaletteTealColor}" Offset="1" />
                                                        </LinearGradientBrush>
                                                    </ProgressBar.Foreground>
                                                </ProgressBar>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Displays -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="Desktop24" Foreground="{DynamicResource PaletteLightBlueBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.Displays}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <ItemsControl x:Name="_displayList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,8">
                                            <StackPanel>
                                                <TextBlock FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap">
                                                    <Run Text="{x:Static resources:Resource.DashboardPage_Display_Title}" /><Run Text=" " /><Run Text="{Binding Index, Mode=OneWay}" /><Run Text=": " /><Run Text="{Binding Model, TargetNullValue={x:Static resources:Resource.Unknown}, Mode=OneWay}" />
                                                </TextBlock>
                                                <TextBlock FontSize="12" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                                    <Run Text="{Binding Width}" />
                                                    <Run Text="×" />
                                                    <Run Text="{Binding Height}" />
                                                    <Run Text=" @ " />
                                                    <Run Text="{Binding RefreshRate}" />
                                                    <Run Text=" " />
                                                    <Run Text="{x:Static resources:Resource.Hz}" />
                                                </TextBlock>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Battery -->
                    <Border x:Name="_batteryInfo" Visibility="Collapsed" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="BatteryCharge24" Foreground="{DynamicResource PaletteGreenBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.BatteryPage_Title}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.BatteryPage_LiveCharge_Title}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_batteryLiveChargeLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>

                            <Grid Margin="0,0,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.BatteryPage_BatteryHealth_Title}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_batteryHealthLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>
                            <Border CornerRadius="3" ClipToBounds="True" Height="6" Background="{DynamicResource ControlStrokeColorDefaultBrush}" Margin="0,0,0,10">
                                <ProgressBar x:Name="_batteryHealthProgressBar" Maximum="100" BorderThickness="0" Background="Transparent" />
                            </Border>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.BatteryPage_DesignCapacity_Title}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_batteryDesignCapacityLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.BatteryPage_FullChargeCapacity_Title}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_batteryFullCapacityLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>

                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.BatteryPage_CycleCount_Title}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_batteryCycleCountLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>

                            <Grid Margin="0,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0">
                                    <Run Text="{x:Static resources:Resource.BatteryPage_ManufactureDate_Title}" /><Run Text=":" />
                                </TextBlock>
                                <TextBlock x:Name="_batteryManufactureDateLabel" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Network -->
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultSolidBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <wpfui:SymbolIcon Symbol="Wifi124" Foreground="{DynamicResource PaletteBlueBrush}" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="{x:Static resources:Resource.NetworkAdapters}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
                            </StackPanel>
                            <ItemsControl x:Name="_networkList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,12">
                                            <TextBlock Text="{Binding Name, TargetNullValue={x:Static resources:Resource.Unknown}}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" />
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                                    <Run Text="{x:Static resources:Resource.MacAddress}" /><Run Text=":" />
                                                </TextBlock>
                                                <TextBlock Text="{Binding MacAddress, TargetNullValue={x:Static resources:Resource.Unknown}}" FontSize="12" Margin="4,0,0,0" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontWeight="Medium" />
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </wpfui:DynamicScrollViewer>

        <wpfui:Snackbar
            x:Name="_snackBar"
            Grid.Row="1"
            HorizontalAlignment="Center"
            CloseButtonEnabled="False"
            Icon="Checkmark24"
            Timeout="1000" />
    </Grid>
</local:BaseWindow>
