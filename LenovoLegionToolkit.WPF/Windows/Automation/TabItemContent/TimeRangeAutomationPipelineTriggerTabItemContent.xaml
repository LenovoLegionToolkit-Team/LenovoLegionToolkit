<UserControl
    x:Class="LenovoLegionToolkit.WPF.Windows.Automation.TabItemContent.TimeRangeAutomationPipelineTriggerTabItemContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Initialized="TimeRangeAutomationPipelineTriggerTabItemContent_Initialized"
    mc:Ignorable="d">
    <StackPanel Margin="16">
        <RadioButton
            x:Name="_sunriseToSunsetRadioButton"
            Margin="0,0,0,8"
            Click="RadioButton_Click"
            Content="{x:Static resources:Resource.TimeRangeAutomationPipelineTriggerTabItemContent_SunriseToSunset}"
            GroupName="A" />

        <RadioButton
            x:Name="_sunsetToSunriseRadioButton"
            Margin="0,0,0,8"
            Click="RadioButton_Click"
            Content="{x:Static resources:Resource.TimeRangeAutomationPipelineTriggerTabItemContent_SunsetToSunrise}"
            GroupName="A" />

        <RadioButton
            x:Name="_timeRangeRadioButton"
            Margin="0,0,0,8"
            Click="RadioButton_Click"
            Content="{x:Static resources:Resource.TimeRangeAutomationPipelineTriggerTabItemContent_CustomRange}"
            GroupName="A" />
        <StackPanel
            x:Name="_timePickerPanel"
            Margin="28,0,0,0"
            VerticalAlignment="Center"
            IsEnabled="False"
            Orientation="Vertical">
            <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                <Label
                    Width="50"
                    VerticalContentAlignment="Center"
                    Content="{x:Static resources:Resource.TimeRangeAutomationPipelineTriggerTabItemContent_From}" />
                <ui:NumberBox
                    x:Name="_startTimePickerHours"
                    ClearButtonEnabled="False"
                    MaxDecimalPlaces="0"
                    Maximum="23"
                    Minimum="0" />
                <Label
                    Margin="4,0,4,0"
                    VerticalContentAlignment="Center"
                    Content=":" />
                <ui:NumberBox
                    x:Name="_startTimePickerMinutes"
                    ClearButtonEnabled="False"
                    MaxDecimalPlaces="0"
                    Maximum="59"
                    Minimum="0" />
                <Label
                    Margin="4,0,4,0"
                    VerticalContentAlignment="Center"
                    Content=":" />
                <ui:NumberBox
                    x:Name="_startTimePickerSeconds"
                    ClearButtonEnabled="False"
                    MaxDecimalPlaces="0"
                    Maximum="59"
                    Minimum="0" />
                <Label
                    Margin="8,0,0,0"
                    VerticalContentAlignment="Center"
                    Content="(HH:MM:SS)" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label
                    Width="50"
                    VerticalContentAlignment="Center"
                    Content="{x:Static resources:Resource.TimeRangeAutomationPipelineTriggerTabItemContent_To}" />
                <ui:NumberBox
                    x:Name="_endTimePickerHours"
                    ClearButtonEnabled="False"
                    MaxDecimalPlaces="0"
                    Maximum="23"
                    Minimum="0" />
                <Label
                    Margin="4,0,4,0"
                    VerticalContentAlignment="Center"
                    Content=":" />
                <ui:NumberBox
                    x:Name="_endTimePickerMinutes"
                    ClearButtonEnabled="False"
                    MaxDecimalPlaces="0"
                    Maximum="59"
                    Minimum="0" />
                <Label
                    Margin="4,0,4,0"
                    VerticalContentAlignment="Center"
                    Content=":" />
                <ui:NumberBox
                    x:Name="_endTimePickerSeconds"
                    ClearButtonEnabled="False"
                    MaxDecimalPlaces="0"
                    Maximum="59"
                    Minimum="0" />
                <Label
                    Margin="8,0,0,0"
                    VerticalContentAlignment="Center"
                    Content="(HH:MM:SS)" />
            </StackPanel>
            <StackPanel x:Name="_daysOfWeekPanel" Margin="0,16,0,0">
                <CheckBox Click="DayOfWeekCheckBox_OnClick" Tag="{x:Static system:DayOfWeek.Monday}" />
                <CheckBox Click="DayOfWeekCheckBox_OnClick" Tag="{x:Static system:DayOfWeek.Tuesday}" />
                <CheckBox Click="DayOfWeekCheckBox_OnClick" Tag="{x:Static system:DayOfWeek.Wednesday}" />
                <CheckBox Click="DayOfWeekCheckBox_OnClick" Tag="{x:Static system:DayOfWeek.Thursday}" />
                <CheckBox Click="DayOfWeekCheckBox_OnClick" Tag="{x:Static system:DayOfWeek.Friday}" />
                <CheckBox Click="DayOfWeekCheckBox_OnClick" Tag="{x:Static system:DayOfWeek.Saturday}" />
                <CheckBox Click="DayOfWeekCheckBox_OnClick" Tag="{x:Static system:DayOfWeek.Sunday}" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>
